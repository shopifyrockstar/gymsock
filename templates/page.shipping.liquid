{% comment %}
<div class="page-width">
  {% include 'nov-breadcrumb' %}
  <div class="nov__page">
  <div class="container">
    {% section 'nov-page-shipping' %}
    {% if page.content.size > 0 %}
      <div class="rte">
        {{ page.content }}
      </div>
    {% endif %}
    
  </div>
</div>
{% endcomment %}
  
  <div class="page-width">
    {% include 'nov-breadcrumb' %}
    <div class="container">
      {% if page.content.size > 0 %}
      <div class="rte">
        {{ page.content }}
      </div>
      {% endif %}

      {% section 'nov-page-shipping' %}
    </div>
  </div>